{
  "name": "@aips/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "typecheck": "mypy .",
    "test": "pytest",
    "lint": "ruff check .",
    "format": "ruff format ."
  }
}
